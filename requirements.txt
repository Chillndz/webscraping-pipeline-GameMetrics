# =============================================================================
# requirements.txt — Observatoire de la popularité des jeux vidéo
# ENSEA AS Data Science — Projet Web Scraping
# =============================================================================

# ── Scraping ──────────────────────────────────────────────────────────────────
scrapy>=2.11.0
itemadapter>=0.7.0

# ── Nettoyage & Analyse des données ──────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0

# ── Base de données PostgreSQL ────────────────────────────────────────────────
psycopg2-binary>=2.9.9      # Connecteur PostgreSQL pour Python
sqlalchemy>=2.0.0           # ORM — utilisé par Flask + Celery pour les modèles

# ── API REST ──────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-sqlalchemy>=3.1.0     # Intégration SQLAlchemy dans Flask
flask-migrate>=4.0.0        # Migrations de base de données
flasgger>=0.9.7.1           # Documentation Swagger/OpenAPI (bonus niveau Or)

# ── Tâches asynchrones ────────────────────────────────────────────────────────
celery>=5.3.0
redis>=5.0.0                # Client Redis pour Python (broker Celery)
celery[redis]>=5.3.0        # Support Redis dans Celery

# ── Monitoring ────────────────────────────────────────────────────────────────
prometheus-flask-exporter>=0.23.0  # Expose les métriques Flask à Prometheus

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # Lecture du fichier .env
requests>=2.31.0            # Requêtes HTTP (utile pour tester l'API)

# ── Tests (niveau Or obligatoire) ─────────────────────────────────────────────
pytest>=8.0.0
pytest-flask>=1.3.0         # Fixtures Flask pour pytest